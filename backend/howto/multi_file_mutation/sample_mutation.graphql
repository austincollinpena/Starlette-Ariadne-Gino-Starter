type Mutation{
    test(argOne: String!): TestPayload
    testOne(argOne: String!): Boolean
    sampleTwo(input: TestInput): Boolean!
}

input TestInput{
    isCool: Boolean
    isGay: Boolean
}

type TestPayload{
    success: Boolean
    message: String
}